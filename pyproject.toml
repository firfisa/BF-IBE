[project]
name = "bf-ibe-phase1"
version = "0.1.0"
description = "Phase-one architecture and interface model for a BF-IBE enterprise encrypted file distribution PoC."
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
testpaths = ["tests"]
